# Monaco Assets

This folder hosts the extension IDE web workbench.

To enable full Monaco editor features offline, place the Monaco `vs/` distribution under:
`assets/ide/monaco/vs/`

The workbench automatically falls back to a plain textarea editor when Monaco is not present.
